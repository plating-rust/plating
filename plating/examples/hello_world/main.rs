/*
 * Copyright (2021) by Marcel Lambert.
 * This project is dual licensed under either MIT or Apache-2.0.
 */

//use plating::prelude::*;
//use plating::utils::children_list;
//use plating::widget::{AppOutlet, WindowOutlet};
use plating::PlatingResult;


fn main() -> PlatingResult<()> {
    /*
    let window = <DefaultSystem as System>::Window::<(), _, _>::new(
        (),
        WindowOutlet {
            children: (),
            menu:     (),
        },
    )?;
    let mut app = <DefaultSystem as System>::App::new_with_state(
        (),
        (),
        AppOutlet {
            children: children_list!(window),
        },
    )?;*/

    //app.run()
    Ok(())
}
